import React, { Component } from 'react'

export default class ProductList extends Component {
    state={
        isPurchase: false
    }
    
    render() {
        return (
            // <li onClick={()=> this.props.isPurchased.fruit}>
            //     {this.props.fruit.item} {this.props.fruit.brand} {this.props.fruit.quantity}
            //     {this.props.fruit.units}{this.state.isPurchase ? <span> is purchased!</span> : ''}
                 
            // </li>
            <p>hi</p>
        )
    }
}
